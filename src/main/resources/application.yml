server:
  port: 8080

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/pastebin_db
    username: root
    password: tanishashah
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect

  thymeleaf:
    cache: false

logging:
  level:
    org.hibernate.SQL: OFF
